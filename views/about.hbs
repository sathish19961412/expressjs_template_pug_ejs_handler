 <!-- About section-->
        <section id="page">
            <div class="container px-4">
                <div class="row gx-4 justify-content-center">
                    <div class="col-lg-8">
                        <h2>My name is Sathish. I will Practices you the follwing courses</h2>
                        <ul>
                            {{#if courseExists}}
                                {{#each courses}}
                                    <li>{{this.name}}</li>
                                {{/each}}
                            {{else}}
                                <p class="lead">No courses available</p>
                            {{/if}}
                        </ul>
                        
                    </div>
                </div>
            </div>
        </section>